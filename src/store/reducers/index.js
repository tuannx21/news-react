const initialState = {
  articles: [],
  isLoading: true,
  isErrors: false
}

const rootReducer = (state, action) => {
  switch(action.type) {
    
    default:
      return state
  }
}

export default rootReducer